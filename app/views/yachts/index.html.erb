<%= render 'shared/navbar' %>
<% if user_signed_in? %>
<% end %>
<!--Search a Yacht-->
      <div class="searchbox">
        <div class="row">
          <div class="col-md-5"><input type="text" class="form-control" placeholder="Where to set sail?"></div>
          <div class="col-md-3">
            <select class="form-control category">
              <option>Category</option>
              <option>Sailing Yacht</option>
              <option>Catamaran Yacht</option>
              <option> Luxury Yacht</option>
              <option>Sports Yacht</option>
            </select>
          </div>
          <div class="col-md-1"><input type="button" class="btn btn-primary" class="form-control" value="Search"></div>
        </div>
      </div>
    </div>
<!--Search a Yacht-->
   <div class="main-container">
   <hr>
   <div class="cards">
      <% @yachts.each do |yacht| %>
      <div class="card">
         <%= link_to yacht_path(yacht) do %>
         <img src="<%= yacht.url %>" alt="Yacht19" class="card-img-top">
         <% end %>
         <div class="card-trip-infos">
         <div>
            <h2><%= yacht.name %></h2>
            <p><%= yacht.description %></p>
            <p><strong> <%= yacht.yacht_type %></strong></p>
            <p><em><%= yacht.location %></em></p>
         </div>
           <h2 class="card-trip-pricing">£<%= yacht.rental_price%> per night</h2>
            <!-- <img src="https://kitt.lewagon.com/placeholder/users/krokrob" class="card-trip-user avatar-bordered" /> -->
         </div>
      </div>
      <% end %>
         </div>
          <div id="map"style="width: 100%; height: 600px;"
     data-markers="<%= @markers.to_json %>"
     data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
     </div>
   </div>
   <!-- Change the token below (https://www.mapbox.com/account/access-tokens/) -->
  
</div>
